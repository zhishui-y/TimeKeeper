// 12px body text plus the table cell's 12px padding on each side keeps two
// Chinese characters visible without letting a resizable column collapse.
export const MIN_RESIZABLE_TABLE_COLUMN_WIDTH = 48;
export const MAX_RESIZABLE_TABLE_COLUMN_WIDTH = 480;
